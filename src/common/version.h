// Declares the shared SPRING2 version string embedded in the CLI, metadata,
// and tests.

#ifndef SPRING_VERSION_H_
#define SPRING_VERSION_H_

namespace spring {
constexpr char VERSION[] = "1.3.6";
}

#endif // SPRING_VERSION_H_
